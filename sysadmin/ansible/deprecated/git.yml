---
- hosts: servers
             
  tasks:
    - name: git config
      git_config:
        name: user.name
        value: "Vishal Apte"
    - name: git config
      git_config:
        name: user.email
        value: "vishalapte@gmail.com"
    - name: git config
      git_config:
        name: core.editor
        value: vim
    - name: git config
      git_config:
        name: color.ui
        value: auto
...
